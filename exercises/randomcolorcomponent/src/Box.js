import React from 'react';

const Box = (props) => {
    return (
        <div style={{backgroundColor: '#'+props.color}}className='mainBox'>
            
        </div>
    );
};

export default Box;